<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        xmlns:VM="clr-namespace:View.ViewModels"
        xmlns:converters="clr-namespace:View.Converters"
        xmlns:controls="clr-namespace:View.UserControls"
        mc:Ignorable="d"
        Icon="/ContactsApp_96x96.ico"
        Title="Contacts" 
        Height="600" 
        Width="700"
        MinHeight="400"
        MinWidth="500">
    <Window.DataContext>
        <VM:MainWindowViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <converters:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <BitmapImage x:Key="DefaultPhoto" UriSource="/photo_placeholder_100x100.png"/>
    </Window.Resources>
    <Grid>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="3"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" Background="#f8f8f8">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" 
                                   Content="Find:" 
                                   Padding="5,0,5,0"
                                   VerticalAlignment="Center"/>
                            <TextBox Grid.Column="1"
                                     VerticalAlignment="Center"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                        </Grid>
                        <ListBox Grid.Row="1" 
                                 Margin="0,0,0,8"
                                 ItemsSource="{Binding FilteredContacts}"
                                 SelectedItem="{Binding SelectedContact}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Black" 
                                            Padding="5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" 
                                                    Width="50" 
                                                    Height="50" 
                                                    CornerRadius="0"
                                                    Background="#f0f0f0"
                                                    Margin="0,0,5,0">
                                                <Image Source="{Binding PhotoBytes, 
                                                    Converter={StaticResource ByteArrayToImageConverter},
                                                    TargetNullValue={StaticResource DefaultPhoto},
                                                    FallbackValue={StaticResource DefaultPhoto}}"
                                                       Width="50" 
                                                       Height="50"
                                                       Stretch="UniformToFill"/>
                                            </Border>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding Name}" 
                                                           FontWeight="SemiBold"
                                                           FontSize="12"/>
                                                <TextBlock Text="{Binding PhoneNumber}" 
                                                           FontSize="10"
                                                           Foreground="Gray"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" 
                                    Content="Add" 
                                    Height="25" 
                                    Margin="0,0,3,0"
                                    Command="{Binding AddCommand}">
                                <Button.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Add random contact" 
                                                  Command="{Binding AddRandomCommand}"/>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>
                            <Button Grid.Column="1" 
                                    Content="Edit" 
                                    Height="25" 
                                    Margin="3,0,3,0"
                                    Command="{Binding EditCommand}"/>
                            <Button Grid.Column="2" 
                                    Content="Remove" 
                                    Height="25" 
                                    Margin="3,0,0,0"
                                    Command="{Binding RemoveCommand}"/>
                        </Grid>
                    </Grid>
                </Grid>
                <GridSplitter Grid.Column="1" 
                              Width="3" 
                              Background="White"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"/>
                <Grid Grid.Column="2" Background="White">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <controls:ContactUserControl Grid.Row="0" DataContext="{Binding EditableContact}"/>
                        <StackPanel Grid.Row="1" 
                                    Orientation="Horizontal"
                                    VerticalAlignment="Top"
                                    HorizontalAlignment="Left"
                                    Margin="5">
                            <Button Content="Clear Photo" 
                                    Width="75" 
                                    Height="25"
                                    Margin="0,0,10,0"
                                    Command="{Binding ClearPhotoCommand}"
                                    Visibility="{Binding IsApplyVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Content="Cancel" 
                                    Width="75" 
                                    Height="25"
                                    Margin="0,0,10,0"
                                    Command="{Binding CancelCommand}"
                                    Visibility="{Binding IsApplyVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            <Button Content="Apply" 
                                    Width="75" 
                                    Height="25"
                                    Margin="0,0,10,0"
                                    Command="{Binding ApplyCommand}"
                                    Visibility="{Binding IsApplyVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>